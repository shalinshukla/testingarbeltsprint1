<?php

//implement the hook_mail()
function ar_custom_inquiry_mail ($key, &$message, $params) {

    switch ($key) {
      case 'update':
        // Set headers etc
        $message['headers']['Bcc'] = 'shalin.addweb@gmail.com';
        $message['subject'] = $params['subject'];
        $message['body'][] = $params['message'];
        break;
    }
  }